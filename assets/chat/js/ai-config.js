/* Endpoint public du Worker Cloudflare. Il sera activé après le premier déploiement. */
window.ADALBERTO_AI_CONFIG = Object.freeze({
  endpoint: "https://adalberto-cv-assistant.adalbertosfurtado.workers.dev/chat",
  timeoutMs: 12000,
  maxHistoryMessages: 6
});
